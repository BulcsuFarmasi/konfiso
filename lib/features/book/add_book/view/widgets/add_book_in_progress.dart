import 'package:flutter/material.dart';

class AddBookInProgress extends StatelessWidget {
  const AddBookInProgress({super.key});

  @override
  Widget build(BuildContext context) {
    return const CircularProgressIndicator();
  }
}
